const arr = [10,20];
console.log(arr.pop());

// 제일 마지막 요소 넣기
// push()
// 제일 마지막 요소 가져오기
// pop()

const file = "aa.jpg";
// ['aa', 'jpg' ]
console.log(file.split(".").pop());

const uuid = crypto.randomUUID();
console.log(uuid);
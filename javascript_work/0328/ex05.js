const arr = [1, 2, 3, 4, 5];

// filter 1,3,5
// return true item 반환
// return fasle 이면 반환하지 않음
// 반환된것을 배열로 만들기
// console.log(Boolean(1));
// console.log(Boolean(0));

// const brr = arr.filter((item) => {
//     console.log(`item = ${item}`);
//     return !(item % 2); // 나머지 0,1
//     // return item % 2 == 1; // 나머지 0,1
// });

// console.log(brr);